function Test({ status, accountNumber, accountBalance }) {
  return (
    <div>
      <p>noggin</p>

      <p>{status}</p>
      <p>Account Number: {accountNumber}</p>
      <p>Balance: {accountBalance}</p>
    </div>
  );
}

export default Test;
